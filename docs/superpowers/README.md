# Development workflow

Each chapter is developed on an isolated Git worktree. A task is committed
only after its focused tests pass. Chapter branches are reviewed and merged
without squashing so the implementation history remains inspectable.

